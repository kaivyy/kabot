# v0.6.2 Release Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Cut Kabot v0.6.2 by promoting the current unreleased fixes into a release, updating package metadata, and pushing the release/tag to GitHub.

**Architecture:** This release is a metadata and packaging pass over already-implemented work. We keep the code changes intact, move the current changelog content into a dated v0.6.2 release entry, verify package/version consistency, then tag and push so GitHub/PyPI automation can run.

**Tech Stack:** Python, Hatchling, pytest, Ruff, Git, GitHub CLI.

---

### Task 1: Capture the release plan

**Files:**
- Create: `docs/plans/2026-03-10-v0.6.2-release-plan.md`

**Step 1: Write the plan file**
- Save this plan so the release steps are documented in-repo.

**Step 2: Verify the file exists**
- Run: `Get-Item docs/plans/2026-03-10-v0.6.2-release-plan.md`
- Expected: file metadata prints successfully.

### Task 2: Bump version metadata

**Files:**
- Modify: `pyproject.toml`
- Modify: `kabot/__init__.py`

**Step 1: Update project version**
- Change `0.6.1` to `0.6.2` in package metadata and runtime version export.

**Step 2: Verify the diff**
- Run: `rg -n "0\.6\.2" pyproject.toml kabot/__init__.py`
- Expected: both files report `0.6.2`.

### Task 3: Promote changelog to a dated release

**Files:**
- Modify: `CHANGELOG.md`

**Step 1: Convert current `Unreleased` content into `0.6.2`**
- Add dated `## [0.6.2] - 2026-03-10`
- Keep the detailed fix notes for issue #2, continuity, weather, skills, and smoke harness.
- Leave a fresh `## [Unreleased]` stub at the top.

**Step 2: Verify the changelog headings**
- Run: `rg -n "^## \[(Unreleased|0\.6\.2|0\.6\.1)" CHANGELOG.md`
- Expected: `Unreleased`, `0.6.2`, and historical `0.6.1` headings all appear in order.

### Task 4: Verify repo references for the current release

**Files:**
- Inspect: repository-wide text references

**Step 1: Search for old version references**
- Run: `rg -n "0\.6\.1|v0\.6\.1" -S .`
- Expected: only historical references that should remain (for example the old changelog section), not current package metadata.

### Task 5: Verify release quality

**Files:**
- No file changes

**Step 1: Run lint**
- Run: `ruff check .`
- Expected: `All checks passed!`

**Step 2: Run tests**
- Run: `pytest tests -q`
- Expected: full suite passes.

**Step 3: Build artifacts**
- Run: `python -m build --sdist --wheel --outdir .tmp-dist-check-062`
- Expected: sdist and wheel for `0.6.2` are created successfully.

### Task 6: Commit, tag, and push

**Files:**
- Commit all tracked release changes

**Step 1: Commit**
- Run: `git add -A && git commit -m "release: ship kabot 0.6.2"`
- Expected: commit created.

**Step 2: Tag**
- Run: `git tag v0.6.2`
- Expected: annotated or lightweight tag exists locally.

**Step 3: Push branch and tag**
- Run: `git push origin main && git push origin v0.6.2`
- Expected: branch and tag are published.

**Step 4: Verify remote state**
- Run: `git status --short --branch`
- Expected: branch no longer ahead once pushes complete.

### Task 7: Verify GitHub/PyPI release automation

**Files:**
- No file changes

**Step 1: Push tag-triggered release**
- Ensure `v0.6.2` tag is pushed so GitHub Actions publishes to PyPI.

**Step 2: Verify workflow success**
- Run: `gh run list --workflow "Publish to PyPI" --limit 5`
- Expected: latest run for `v0.6.2` is `success`.

**Step 3: Verify PyPI version**
- Check the latest published package version for `kabot` and confirm `0.6.2` is live.
