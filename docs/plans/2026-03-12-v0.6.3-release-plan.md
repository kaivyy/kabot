# v0.6.3 Release Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Cut Kabot v0.6.3 by promoting the current MCP, continuity, delivery, and Skyclaw-style runtime hardening into a documented release with synced package metadata, docs, and release automation inputs.

**Architecture:** This release packages work that is already implemented across runtime, CLI, docs, and smoke coverage. We keep the behavioral changes intact, update version metadata and public docs to match the new MCP/runtime story, promote the current `Unreleased` notes into a dated `0.6.3` changelog entry, then verify build/docs/test surfaces before committing, tagging, and pushing.

**Tech Stack:** Python, Hatchling, MkDocs Material, pytest, Git, GitHub Actions, PyPI trusted publishing.

---

### Task 1: Capture the release plan

**Files:**
- Create: `docs/plans/2026-03-12-v0.6.3-release-plan.md`

**Step 1: Save the plan**
- Keep the release workflow documented in-repo before touching metadata and docs.

**Step 2: Verify the file exists**
- Run: `Get-Item docs/plans/2026-03-12-v0.6.3-release-plan.md`
- Expected: file metadata prints successfully.

### Task 2: Bump release metadata

**Files:**
- Modify: `pyproject.toml`
- Modify: `kabot/__init__.py`

**Step 1: Update version**
- Change runtime/package version from `0.6.2` to `0.6.3`.

**Step 2: Verify**
- Run: `rg -n "0\.6\.3" pyproject.toml kabot/__init__.py`
- Expected: both files report `0.6.3`.

### Task 3: Promote the changelog into a real release entry

**Files:**
- Modify: `CHANGELOG.md`

**Step 1: Cut the dated release section**
- Leave a fresh `## [Unreleased]` stub.
- Promote the current top-level release notes into `## [0.6.3] - 2026-03-12`.

**Step 2: Verify headings**
- Run: `rg -n "^## \[(Unreleased|0\.6\.3|0\.6\.2)" CHANGELOG.md`
- Expected: headings appear in order.

### Task 4: Update public docs for MCP and runtime parity

**Files:**
- Modify: `README.md`
- Modify: `HOW_TO_USE.MD`
- Modify: `mkdocs.yml`
- Modify: `site_docs/index.md`
- Modify: `site_docs/advanced/index.md`
- Modify: `site_docs/advanced/integrations.md`
- Modify: `site_docs/guide/configuration.md`
- Modify: `site_docs/reference/commands.md`
- Modify: `site_docs/reference/configuration.md`
- Create: `site_docs/advanced/mcp.md`

**Step 1: README**
- Add a compact `What's new in v0.6.3` summary.
- Add MCP quickstart/config/verify commands.
- Explain Skyclaw/OpenClaw-style continuity and evidence-based execution at a user-facing level.

**Step 2: HOW_TO_USE**
- Add an MCP section for beginners/operators.
- Clarify when Kabot uses tools/skills vs normal chat and how continuity/memory now behave.

**Step 3: MkDocs**
- Add MCP page to navigation.
- Keep docs public-facing and practical.

**Step 4: Verify docs references**
- Run: `rg -n "MCP|Skyclaw|continuity|kabot mcp" README.md HOW_TO_USE.MD mkdocs.yml site_docs -S`
- Expected: new coverage appears in the updated docs files.

### Task 5: Verify release quality

**Files:**
- No file changes

**Step 1: Run targeted release tests**
- Run: `python -m pytest tests/test_package_metadata.py tests/cli/test_mcp_commands.py tests/cli/test_commands_module_exports.py tests/cli/test_agent_smoke_matrix.py tests/config/test_loader_env_override.py tests/config/test_mcp_config.py -q`
- Expected: all pass.

**Step 2: Build docs**
- Run: `python -m mkdocs build --strict`
- Expected: docs build succeeds.

**Step 3: Build distributions**
- Run: `python -m build --sdist --wheel --outdir .tmp-dist-check-063`
- Expected: sdist and wheel for `0.6.3` are created.

### Task 6: Commit, tag, and push

**Files:**
- Commit all tracked release changes, excluding local scratch artifacts

**Step 1: Commit**
- Run: `git add ... && git commit -m "release: ship kabot 0.6.3"`
- Expected: commit created.

**Step 2: Tag**
- Run: `git tag v0.6.3`
- Expected: local tag exists.

**Step 3: Push**
- Run: `git push origin main && git push origin v0.6.3`
- Expected: branch and tag are published.

### Task 7: Verify release automation

**Files:**
- No file changes

**Step 1: Verify PyPI workflow**
- Run: `gh run list --workflow "Publish to PyPI" --limit 5`
- Expected: a recent run for `v0.6.3` appears.

**Step 2: Verify docs workflow**
- Run: `gh run list --workflow "Publish Docs" --limit 5`
- Expected: a recent run for `main` appears.

**Step 3: Verify package/version remotely**
- Confirm PyPI shows `0.6.3` after workflow completion.
